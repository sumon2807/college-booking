import React from 'react';
import useCollegeData from '../../Hooks/useCollegeData';
import { useParams } from 'react-router-dom';

const CollegeDetails = ({Cdata}) => {
    console.log(Cdata);
    return (
        <div>
        </div>
    );
};

export default CollegeDetails;